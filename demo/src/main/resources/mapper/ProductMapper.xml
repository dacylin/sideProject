<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.ProductMapper">

    <select id="findAll" resultType="com.example.demo.entity.Product">
        SELECT id, name, description, price, stock, created_at FROM products
    </select>

    <select id="findById" resultType="com.example.demo.entity.Product">
        SELECT id, name, description, price, stock, created_at FROM products WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO products (name, description, price, stock, created_at)
        VALUES (#{name}, #{description}, #{price}, #{stock}, NOW())
    </insert>

    <delete id="deleteById">
        DELETE FROM products WHERE id = #{id}
    </delete>

    <update id="update">
        UPDATE products
        SET name = #{name},
            description = #{description},
            price = #{price},
            stock = #{stock}
        WHERE id = #{id}
    </update>

</mapper>
